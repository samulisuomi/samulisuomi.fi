import React from 'react'

import './Projects.scss'

const PAGE_ID = 'projects'
const ROOT_CLASS = `pages-home-${PAGE_ID}`

export default () => (
  <div className={ROOT_CLASS}>
    <h4 id={PAGE_ID}>
      Fun Projects to Eyeball
    </h4>
    <p>
      Lorem
    </p>
  </div>
)
